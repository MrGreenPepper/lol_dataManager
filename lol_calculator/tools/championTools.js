async function evaluateSkillTab(skillTab, abilityLevel, championStats) {
	//TODO: extension with items and scaling
	/** returns, for a given skillTab and level, the damage the damage type and any specials
	 * 1. get the damage
	 * 2. get the type
	 * 3. get any specials
	 * @skillTab		Object
	 * @abilityLevel	number
	 * @returns: Object {	damage: 	number
	 * 						damageType:	string
	 * 						specials:	array
	 * 	}
	 */
	//1. get the damage
}

async function calculateAbility(ability, stats) {
	/**Connects given stats with an ability and calculates the damage
	 * @param 	ability 	simplified ability
	 * @param	baseStats	stats to calculate the ability value with it
	 * @returns {object}	abilityValues	a summary of the calculated values
	 */

	let abilityValues = {};

	return abilityValues;
}
